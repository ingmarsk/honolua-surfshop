// Rails template that generate JavaScript to be executed in the browser

console.log('POST / Processing by LineItemsController#create as JS');

if ($('#cart tr').length) { 
	console.log('carts');
	$('#cart').show('blind', 1000); 
} else {
	console.log('no carts');
}

// Replace the content of the div#cart from application_html with this HTML
$('#cart').html("<%= escape_javascript render(@cart) %>");

$('#current_item')
		.css({'background-color':'#d9edf7'})
				.animate({'background-color':'#fff'}, 2000);